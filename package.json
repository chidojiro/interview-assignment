{
  "name": "@ffw/randstad-react-components",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "webpack-cli": "^4.5.0"
  },
  "scripts": {
    "build": "rm -rf build && webpack --mode production"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
